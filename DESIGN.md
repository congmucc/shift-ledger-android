# Shift Ledger Compact UI Design Baseline

## 1. 当前设计基准

Shift Ledger 当前 UI 以移动端 Android 体验为主，Web 仅用于预览和快速验收。最终视觉基准是：

- HTML 设计稿：`.design-preview/compact-ledger-ui.html`
- 预览地址：`http://127.0.0.1:4312/compact-ledger-ui.html`
- Flutter 实现预览：`http://127.0.0.1:4300/`

旧的 `PROTOTYPE.html` 已废弃，不再作为视觉或交互参考。

## 2. 设计目标

这是一个个人工时账本，不是企业排班后台。用户打开 App 第一眼要能确认：

- 今天是否已记录、记录了几小时；
- 某个月哪些日期有普通工时、加班、夜班、备注、超长；
- 当前统计范围内总工时、收入、出勤、加班、夜班是否可信；
- 数据是否可导出、可本地备份/恢复、可 WebDAV 同步；
- 大字体下日期、工时、按钮、底部导航不遮挡、不丢失。

设计方向：**紧凑、干净、低噪音、颜色语义唯一、信息密度更高但不牺牲触控和可读性**。

## 3. 核心视觉语言

### 颜色语义

实现位置：`lib/src/ui/theme.dart`。

| 语义 | Token | Hex | 用途 |
| --- | --- | --- | --- |
| 背景 | `background` | `#FCFDFF` | 页面背景 |
| 画布 | `canvas` | `#EEF2F6` | 外层/预览画布 |
| 卡片 | `surfaceRaised` | `#FFFFFF` | 主要卡片、弹层 |
| 软底 | `surfaceSoft` | `#F7F9FC` | 轻量状态块、指标底 |
| 边框 | `hairline` | `#E5E8EE` | 卡片线、分割线 |
| 强边框 | `hairlineStrong` | `#D2D8E2` | 次级数字、弱状态 |
| 主文字 | `ink` | `#0F172A` | 标题、核心数字 |
| 辅助文字 | `muted` | `#667085` | 标签、说明 |
| 更弱文字 | `stone` | `#98A2B3` | 非核心辅助 |
| 普通/主操作 | `primaryBlue` | `#0B66D0` | 普通工时、主按钮、选中态 |
| 加班 | `successGreen` | `#22A65A` | 加班、正向状态 |
| 夜班 | `nightIndigo` | `#5B57D6` | 夜班 |
| 备注/提醒 | `warningOrange` | `#F79009` | 备注、提醒 |
| 超长/危险 | `errorRed` | `#E5484D` | 超长、删除、失败 |

规则：

- 蓝 = 普通工时 / 主操作。
- 绿 = 加班。
- 紫 = 夜班。
- 橙 = 备注 / 提醒。
- 红 = 超长 / 异常 / 删除。
- 不只靠颜色表达状态；日历、列表、汇总都要有文字、点、chip 或语义说明。
- 不使用旧版暖黄纸面、复古手账、重阴影风格。

### 字体与密度

- 页面标题约 23sp，粗字重，短标题优先：`今日 / 日历 / 汇总 / 设置`。
- 核心数字约 22-39sp，使用 tabular figures，允许自动缩放避免溢出。
- 标签约 9.5-12sp，辅助但不能承载唯一关键信息。
- 默认 UI 要紧凑；系统大字体是适配目标，不是默认“大字模式”。
- 触控目标尽量不小于 44dp；按钮、底部导航和重要入口不能为了紧凑而不可点。

## 4. 信息架构

底部导航保留 5 个视觉位置：

1. 首页：今日卡片、本周期进度、三项快捷操作。
2. 日历：月份切换、月摘要、月历/列表、筛选、日期详情。
3. 中间新增：主操作，按当前上下文补一段；日历页按选中日期，其余按今天。
4. 汇总：范围切换、完整日期范围、核心统计、趋势图、工时表、收入组成、计薪依据、导出。
5. 设置：规则与数据两组，包含模板、计薪、规则历史、夜班、发薪周期、CSV、本地备份、WebDAV、最近删除。

## 5. 页面约定

### 首页

- 标题：`今日`。
- 右上角：`补一段`。
- 首屏主卡片：日期、今日总工时、预计收入、普通/加班/夜班/补贴/分段 chip。
- 有记录时展示今天分段；无记录时只保留明确空状态和补今天入口。
- 本周期进度展示总工时、出勤、加班、夜班，不放低频入口。
- 快捷操作只保留：`补今天 / 查日历 / 看汇总`。

### 日历

- 标题：`日历`。
- 月份工具行：上月、`YYYY年M月`、今天、下月。
- 月摘要固定四项：月计、出勤、加班、分段。夜班/备注等不再挤进摘要卡，交给筛选和列表表达。
- 模式切换：月历 / 列表。
- 筛选：全部、加班、夜班、备注、超时，颜色和全局语义一致。
- 月历日期格：日期数字保持干净，底部用小点表达普通/加班/夜班/备注/超长，不让图标遮挡日期。
- 列表：按日期升序，显示日期、段数、总工时、预览分段、收入；多段时折叠为 `+N段`。
- 详情区要随选中日期刷新；补一段默认补当前选中日期。

### 汇总

- 标题：`汇总`。
- 范围切换保留：本月、本周、年度、发薪周期、自定义。
- 必须显示完整范围，例如：`本月自然月 · 2026年5月1日—2026年5月31日`，不能省略年份。
- 核心指标：总工时、预计收入；收入数字可占更大空间。
- 额外指标：出勤、加班、夜班、补贴。
- 趋势图支持总工时、普通、加班、夜班、出勤、收入切换；左轴为小时，右轴为收入。
- 工时表展示最近记录日，日期用 `M/D`，不是不明含义的字母或缩写。
- 收入组成使用条形进度，颜色沿用工时语义。
- 计薪依据保留，作为用户核对收入可信度的兜底信息。

### 设置

- 标题：`设置`。
- 分组标题：`规则`、`数据`。
- 每行采用紧凑设置行：左侧短标签图标，中间标题/摘要，右侧动作或状态。
- 规则组：班次模板、计薪规则、规则历史、夜班规则、发薪周期。
- 数据组：CSV 导出、本地备份/恢复、坚果云 WebDAV、最近删除。
- 本地备份强调本地文件带时间戳；WebDAV 云端保持清洁覆盖。

## 6. 组件约定

### `PageFrame`

- 统一页面标题、右侧动作和列表滚动容器。
- 默认左右 12dp、顶部 8dp、底部给导航栏预留空间。
- 支持 `eyebrow`，但不要滥用；主页面标题保持短。

### `LedgerCard`

- 16dp 圆角。
- 白底、细边框、极轻阴影。
- 用于指标、详情、设置分组、趋势卡、弹窗内容。

### `LedgerPill`

- 用于状态和轻量操作。
- 支持 selected / dense / onTap。
- 文案必须短，例如：`普通 6.5h`、`加班 1h`、`夜班 2次`、`范围 ›`。

### `CompactMetric`

- 用于四宫格小指标。
- 标签短、数字优先。
- 大字体下用 `FittedValueText` 防止溢出。

### `SettingTile`

- 设置页和计薪依据统一使用。
- 可以有 `iconLabel` 或 `icon`。
- 若有 trailing 动作，右侧显示 `动作 ›`；没有 trailing 但可点击，则显示 chevron。

## 7. 可访问性与大字体

必须持续满足：

- 系统最大字体下，日历周几、日期、工时、底部导航不遮挡。
- 日历格不把状态标记盖在日期上。
- 关键按钮有文字或 tooltip/semantics。
- 删除、恢复、覆盖、云端恢复等操作有确认。
- 颜色之外有文字/图标/点/语义辅助。
- 普通字号下主要页面不被底部导航遮挡。

## 8. 验证要求

UI 或交互变更完成前至少运行：

```bash
flutter analyze
flutter test --reporter compact
```

涉及 Web 预览时：

```bash
flutter run -d web-server --web-hostname 127.0.0.1 --web-port 4300
python3 -m http.server 4312 --directory .design-preview
```

## 9. 不再使用的内容

- `PROTOTYPE.html`：旧暖色手账风原型，已废弃并删除。
- 旧 `iOS Neutral` 文档只能作为历史背景，不再覆盖当前 compact UI 基准。
- 不再新增大块说明、图例堆叠、重复入口或默认大字号 UI。
